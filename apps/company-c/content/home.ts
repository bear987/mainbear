/* ============================================================
   GG AUTOS — Home page content.

   Editable values live in data/home.json.
   ============================================================ */
import data from "./data/home.json";

export const hero = data.hero;
export const assembly = data.assembly;
export const paths = data.paths;
export const trust = data.trust;
export const faqs = data.faqs;
