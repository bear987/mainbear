/* ============================================================
   GG AUTOS — Wholesale page content.
   Values the owner has not confirmed are deliberately shown in
   brackets rather than guessed at.

   Editable values live in data/wholesale.json.
   ============================================================ */
import data from "./data/wholesale.json";

export const wholesale = data.wholesale;
export const audiences = data.audiences;
export const howItWorks = data.howItWorks;
export const terms = data.terms;
export const faqs = data.faqs;
