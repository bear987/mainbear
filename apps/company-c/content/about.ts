/* ============================================================
   GG AUTOS — About page content.
   Roles, not invented people. Bracketed values are placeholders
   the owner replaces with real figures.

   Editable values live in data/about.json.
   ============================================================ */
import data from "./data/about.json";

export const about = data.about;
export const story = data.story;
export const mission = data.mission;
export const whatWeDo = data.whatWeDo;
export const stats = data.stats;
export const values = data.values;
export const team = data.team;
export const groupBand = data.groupBand;
