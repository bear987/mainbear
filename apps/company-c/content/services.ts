/* ============================================================
   GG AUTOS — Services page content.
   Coupling, attachment and joining are the same service, named
   all three ways because buyers search all three.

   Editable values live in data/services.json.
   ============================================================ */
import data from "./data/services.json";

export const service = data.service;
export const process = data.process;
export const quality = data.quality;
export const buyerMeaning = data.buyerMeaning;
export const supply = data.supply;
export const catalogue = data.catalogue;
export const faqs = data.faqs;
